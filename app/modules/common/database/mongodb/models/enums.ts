export enum WorkStatus {
  DRAFT = "DRAFT",
  PLANNING = "PLANNING",
  ACTIVE = "ACTIVE",
  ARCHIVED = "ARCHIVED",
}

export enum ProgramStatus {
  DRAFT = "DRAFT",
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum ProgramType {
  OPEN = "OPEN",
  CUSTOM = "CUSTOM",
  EXTERNAL = "EXTERNAL",
}

export enum SectionWidth {
  center = "center",
  full = "full",
}

export enum BannerImagePosition {
  top = "top",
  bottom = "bottom",
  center = "center",
  left = "left",
  right = "right",
}

export enum CohortSectionType {
  overview_section = "overview_section",
  benefits_section = "benefits_section",
  design_curriculum_section = "design_curriculum_section",
  faculty_section = "faculty_section",
  industry_experts_section = "industry_experts_section",
  statistics_section = "statistics_section",
  certification_section = "certification_section",
  testimonial_section = "testimonial_section",
  who_should_apply_section = "who_should_apply_section",
  custom_section = "custom_section",
}

export enum ContentActionBlockType {
  primary = "primary",
  secondary = "secondary",
  link = "link",
}

export enum FacultyType {
  PROFESSOR_TIER_1 = "PROFESSOR_TIER_1",
  PROFESSOR_TIER_2 = "PROFESSOR_TIER_2",
  PROFESSOR_TIER_3 = "PROFESSOR_TIER_3",
  COACH = "COACH",
  SUCCESS_COACH = "SUCCESS_COACH",
}

export enum Role {
  Admin = "Admin",
  User = "User",
}

